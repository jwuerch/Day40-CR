<!DOCTYPE html>
<html>
    {% include 'header.html' %}
    <body>
        <div class="container-fluid">
            <h3>Here are your current stylists in your database:</h3>
            <ul>
            {% for stylist in stylists %}
                <li>{ stylist }</li>

            {% endfor %}
            </ul>
            <h4>Fill out the form to add a stylist to your database:</h4>
            <form action="/add_stylist" method="post">
                <div class="form-group">
                    <label for="name">Stylist Name</label>
                    <input id="name" name="name" class="form-select" type="text">
                </div>

                <div class="form-group">
                    <label for="location">Stylist Location</label>
                    <input id="location" name="location" class="form-select" type="text">
                </div>
            </form>
        </div>
    </body>
</html>
